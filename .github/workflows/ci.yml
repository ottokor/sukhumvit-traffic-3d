name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Syntax check
        run: |
          for f in corridor.js map.js scripts/*.mjs tests/*.mjs; do node --check "$f"; done
      - name: Unit tests
        run: npm test
      - name: Tile list and TomTom budget
        run: node scripts/refresh-traffic.mjs --dry-run
      - name: Frontend must never talk to TomTom directly or contain a key
        run: |
          if grep -nE 'api\.tomtom\.com|TOMTOM_API_KEY|key=' index.html map.js style.css corridor.js; then
            echo "::error::Frontend references TomTom or an API key. Traffic must come from /traffic/ only."
            exit 1
          fi
          echo "ok"
